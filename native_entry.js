// Native entry point for Titan Runtime
// This file is loaded by titan-server extensions.rs
// It does not need to export anything, as the native functions are injected into t["@titanpl/core"]
// and the bundle brings its own index.js for the API wrapper.
(function () {
    // Native entry loaded
})();
